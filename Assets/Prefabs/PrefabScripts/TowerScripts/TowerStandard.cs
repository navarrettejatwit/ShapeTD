using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//This tower is the poop tower, it fires a ballista arrow found in directory assets/prefabs/TowerFabs/arrow
//this tower does very minamal damage.
public class TowerStandard : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
